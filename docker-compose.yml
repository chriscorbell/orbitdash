services:
  orbitdash:
    container_name: orbitdash
    build: .
    ports:
      - "7770:3000"
    volumes:
      - orbitdash-data:/data
    environment:
      - PORT=3000
    restart: unless-stopped

volumes:
  orbitdash-data:
